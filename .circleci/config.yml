version: 2.1

executors:
  python:
    docker:
      - image: cimg/python:3.8

jobs:
  # run_lint:
  #   executor: python
  #   steps:
  #     - checkout
  #     - run: make lint
  run_codestyle:
    executor: python
    steps:
      - checkout
      - run: make style
  run_tests:
    executor: python
    steps:
      - checkout
      - run: make test
      - store_test_results:
          path: test-results

workflows:
  build:
    jobs:
      # - run_lint
      - run_codestyle
      - run_tests
