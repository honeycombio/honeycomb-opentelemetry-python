version: 2.1

executors:
  python:
    docker:
      - image: cimg/python:3.8

jobs:
  run_lint:
    executor: python
    steps:
      - checkout 
      - run: poetry install --only dev
      - run: poetry run pylint src tests

workflows:
  build:
    jobs:
      - run_lint