version: 2.1

executors:
  python:
    docker:
      - image: cimg/python:3.8

jobs:
  run_lint:
    executor: python
    steps:
      - checkout 
      - run: poetry install --only dev
      - run: poetry run pylint src tests
  run_codestyle:
    executor: python
    steps:
      - checkout 
      - run: poetry install --only dev
      - run: poetry run pdcodestyle src tests
  run_tests:
    executor: python
    steps:
      - checkout 
      - run: poetry install
      - run: mkdir test-results
      - run: poetry run pytest tests --junitxml=test-results/junit.xml

workflows:
  build:
    jobs:
      - run_lint
      - run_codestyle
      - run_tests